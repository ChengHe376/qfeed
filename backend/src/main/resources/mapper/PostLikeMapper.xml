<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qfeed.post.mapper.PostLikeMapper">

    <insert id="insertLike">
        insert into post_likes (
            post_id,
            user_id
        ) values (
                     #{postId},
                     #{userId}
                 )
    </insert>

    <delete id="deleteLike">
        delete from post_likes
        where post_id = #{postId}
          and user_id = #{userId}
    </delete>

    <select id="existsLike" resultType="java.lang.Long">
        select 1
        from post_likes
        where post_id = #{postId}
          and user_id = #{userId}
            limit 1
    </select>

    <select id="findLikedPostIds" resultType="java.lang.Long">
        select post_id
        from post_likes
        where user_id = #{userId}
        and post_id in
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </select>

</mapper>