<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qfeed.auth.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenRecordMap"
               type="com.qfeed.auth.security.RefreshTokenRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="jti" property="jti"/>
        <result column="token_hash" property="tokenHash"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="revoked_at" property="revokedAt"/>
        <result column="replaced_by_jti" property="replacedByJti"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO auth_refresh_tokens (user_id, jti, token_hash, expires_at)
        VALUES (#{userId}, #{jti}, #{tokenHash}, #{expiresAt})
    </insert>

    <select id="findByJti" resultMap="RefreshTokenRecordMap">
        SELECT id, user_id, jti, token_hash, expires_at, revoked_at, replaced_by_jti, created_at
        FROM auth_refresh_tokens
        WHERE jti = #{jti}
        LIMIT 1
    </select>

    <update id="revokeByJti">
        UPDATE auth_refresh_tokens
        SET revoked_at = #{revokedAt},
        replaced_by_jti = #{replacedByJti}
        WHERE jti = #{jti}
        AND revoked_at IS NULL
    </update>

    <update id="revokeByJtiForce">
        UPDATE auth_refresh_tokens
        SET revoked_at = #{revokedAt}
        WHERE jti = #{jti}
    </update>

    <update id="revokeAllByUserId">
        UPDATE auth_refresh_tokens
        SET revoked_at = #{revokedAt}
        WHERE user_id = #{userId}
        AND revoked_at IS NULL
    </update>

</mapper>